syntax = "proto3";

package protodb.database;

message ListDatabasesRequest {
}

message ListDatabasesResponse {
    enum FailureCode {
        NO_ERROR = 0;
        INTERNAL_ERROR = 1;
    }
    FailureCode failure_code = 1;

    repeated string databases = 2;
}